
#include <opencv2/opencv.hpp>
#include "OpenCVSample.h"

using namespace cv;

int main()
{
    Mat img = imread("test_img.jpg");
    processImage(img);
    imshow("test_window", img);
    return 0;
}
